name: nightly

on:
  push:
    branches: [master]

permissions:
  contents: write

jobs:
  mingw:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/cache@v4
        with:
          path: subprojects/packagecache
          key: ${{ hashFiles('subprojects/*.wrap') }}

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-mingw-w64 dos2unix nasm meson ninja-build

      - name: Build
        run: ./.ci/nightly.sh

      - name: Update release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VER="$(cat q2pro-build/version.txt)"
          if gh release view nightly >/dev/null 2>&1; then
            gh release edit nightly -t "Nightly" -n "Latest nightly build ${VER}." --latest
          else
            gh release create nightly --title "Nightly" --notes "Latest nightly build ${VER}." --target "${GITHUB_SHA}" --latest
          fi
          gh release upload nightly q2pro-build/*.zip q2pro-build/*.tar.gz q2pro-build/version.txt --clobber

      - name: Update tag
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.git.updateRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: "tags/nightly",
              sha: context.sha,
              force: true
            })
